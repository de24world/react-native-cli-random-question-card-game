import {StyleSheet} from 'react-native';

export const globalStyles = StyleSheet.create({
  container: {
    flext: 1,
    padding: 20,
  },
  titleText: {
    // fontFamily: 'sample'
    fontSize: 18,
  },
});
